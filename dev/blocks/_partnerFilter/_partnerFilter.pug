mixin _partnerFilter(modificator, extraClasses)
	- classList = '_partnerFilter'

	if modificator
		each mod in modificator
			- classList = classList + ' _partnerFilter--' + mod

	if extraClasses
		- classList = classList + ' ' + extraClasses

	div(class=classList style="border: 1px solid red; padding: 20px 0; text-align: center;") Фильтр поиска напарника
