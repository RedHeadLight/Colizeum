extends ../layouts/main.pug

block variables 
	- var title = 'Видео-обучение'

block content
	article.video
		.container.container.video__container
			include ../blocks/backButton/backButton.pug
			+backButton(['absolute'], 'video__back', 'Вернуться к заполнению', 'out')

			include ../blocks/pageHeading/pageHeading.pug
			+pageHeading([], 'video__heading', 'Видео-обучение')

			include ../blocks/videoVideo/videoVideo.pug
			+videoVideo([])
